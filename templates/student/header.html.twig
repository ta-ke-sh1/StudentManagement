{% extends 'base.html.twig' %}
{% block sidebar %}
	<!-- Sidebar -->
	<div class="l-navbar" id="nav-bar">
		<nav class="nav">
			<div>
				<a href="{{ path('homepage') }}" class="nav_logo">
					<img src="{{ asset('logo/logo_blue.png') }}" alt=""/>
				</a>
				<a href="#" class="nav_link">Profile</a>
				<hr/>
				<br/>
				<div class="nav_list">
					<a href="{{ path('student_list') }}" class="nav_link active">
						Students
					</a>
					<a href="{{ path('classFGW_list') }}" class="nav_link">
						Class
					</a>
					<a href="{{ path('subject_list') }}" class="nav_link">
						Subject
					</a>
					<hr id="btmline"/>
				</div>
			</div>
			<a href="{{ path('app_logout') }}" class="nav_link">Log Out</a>
		</nav>
	</div>
	<!-- Sidebar -->
{% endblock %}
