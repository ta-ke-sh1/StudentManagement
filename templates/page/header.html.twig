{% extends 'base.html.twig' %}
{% block sidebar %}
	<!-- Sidebar -->
	<div class="l-navbar" id="nav-bar">
		<nav class="nav">
			<div>
				<a href="{{ path('homepage') }}" class="nav_logo">
					<img src="{{ asset('logo/logo_blue.png') }}" alt=""/>
				</a>
				<a href="{{ path('user_list') }}" class="nav_link">Profile</a>
				<hr/>
				<br/>
				<div class="nav_list">
					<a href="{{ path('student_list') }}" class="nav_link">
						Students
					</a>
					<a href="{{ path('teacher_list') }}" class="nav_link">
						Teacher
					</a>
					<a href="{{ path('classFGW_list') }}" class="nav_link active">
						Class
					</a>
					<a href="{{ path('subject_list') }}" class="nav_link">
						Subject
					</a>
                    <a href="{{ path('schedule_list') }}" class="nav_link">
						Subject Schedule
					</a>
					<hr id="btmline"/>
				</div>
			</div>
			<a href="{{ path('app_logout') }}" class="nav_link">Log Out</a>
		</nav>
	</div>
	<!-- Sidebar -->
{% endblock %}
